{
  "name": "gateway-lambda",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && yarn ncc build src/index.js -so dist -e aws-sdk"
  },
  "dependencies": {
    "apollo-server-lambda": "^3.4.0",
    "apollo-server-core": "^3.4.0",
    "cross-fetch": "^3.1.4",
    "graphql": "^15.6.1",
    "@graphql-tools/wrap": "^8.2.0",
    "@graphql-tools/delegate": "^8.3.0",
    "lambda-log": "^3.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.33.0",
    "aws-sdk": "^2.1044.0",
    "eslint": "^8.4.1"
  }
}